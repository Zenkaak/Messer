{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/db run push || true && pnpm --filter @workspace/gsm-africa run build && node scripts/build-vercel-handler.mjs",
  "framework": null,
  "crons": [
    {
      "path": "/api/admin/cron/refresh-models",
      "schedule": "0 4 */2 * *"
    }
  ]
}
